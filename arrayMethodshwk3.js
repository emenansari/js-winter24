// 1.
function age(number) {
  if (age >= 65) {
    return "Special Discount";
  } else {
    return "Not Allowed";
  }
}

// 2.
const newArrc = arr.map((element) => {
  return element * 100;
});

// 3.
